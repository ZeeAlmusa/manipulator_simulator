clear, clc;
[ax, f] = create_axis(1);
button = 1;
q = [0, 0, 0]';
L1 = [1, 0, 0];
L2 = [2, 0, 0];
L3 = [1, 0, 0];
L = {L1, L2, L3};
% while sum(button) <=3   % read ginputs until a mouse right-button occurs
%     [x,y, button] = ginput(1);
%     %theta = 90;
%     desired_pos = [rand(1,3)*3]'%, theta]';
%     q = arm_solve(desired_pos, q);
% end

desired_pos = [1,  2 , 1]';
[q, sol] = arm_solve(desired_pos, q, L);

function [q, sol] = arm_solve(desired_pos, q, L)

%initialization for the view
ax = create_axis(1);
path = [];
draw_target(desired_pos);

[q, sol] = get_IK(q, L, desired_pos, ax);
cl
move(q, nextpoint);




end




